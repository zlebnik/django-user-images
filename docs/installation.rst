============
Installation
============

At the command line::

    $ easy_install django-user-images

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-user-images
    $ pip install django-user-images
